rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side reads and writes by default.
    // Data access should go through secured backend API routes.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
